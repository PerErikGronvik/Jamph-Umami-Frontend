# @navikt packages are published to the public npm registry.
# This override prevents a user-level .npmrc that routes @navikt to
# npm.pkg.github.com (GitHub Packages) from causing 401 errors.
@navikt:registry=https://registry.npmjs.org
